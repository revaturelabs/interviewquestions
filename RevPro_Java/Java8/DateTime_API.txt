#P1
##EASY
###BC
1. In _____ package does the Date Time API reside in Java 8?

A. java.time package
B. java.util package
C. java.date package
D. none of the above

Answer: A

#P1
##EASY
###BC
2. What are the classes used in Date and Time API in java 8?

A. LocalDateTime, RemoteDateTime 
B. LocalDateTime , ZonedDateTime 
C. RemoteDateTime , ZonedDateTime  
D. None of the above
 
Answer: B  
Explanation:LocalDateTime is the class in the date-time API when there is no complexity in timezone handling. ZonedDateTime is also a class in the date-time API used to deal with various timezones.

#P1
##EASY
###BC
3. _____ can be used when there is no need for time zones.

A. LocalDateTime  
B. ZonedDateTime 
C. RemoteDateTime
D. All the above

Answer: A 

#P1
##EASY
###BC
4. ZonedDateTime API is used ___ .

A. when we are not considering time zones
B. when we are not considering date zones
C. when we are considering time zones
D. when we are considering date zones

Answer: C 

#P1
##EASY
###BC
5. Which of the following syntax helps to format the date and time in this "dd-MM-yyyy HH:mm:ss" format?

A. TimeFormatter format = DateTimeFormatter.ofPattern("dd-MM-yyyy HH:mm:ss");  
B. Date format = DateTimeFormatter.ofPattern("dd-MM-yyyy HH:mm:ss");  
C. DateTimeFormatter format = DateTimeFormatter. date("dd-MM-yyyy HH:mm:ss");  
D. DateTimeFormatter format = DateTimeFormatter.ofPattern("dd-MM-yyyy HH:mm:ss");  

Answer: D

#IGNORE
6. Which is not the drawback of existing date and time API before Java 8?
 
A. It is thread safe 
B. It was poorly designed with less number of features
C. Need to write a seperate code for handling time zone logic in older version. 
D. None of the above

Answer: A

#P2
##EASY
###BC
7. To define an amount of time with date-based values (years, months, days), use the _____ class.

A. Duration
B. Period
C. Seconds
D. Days

Answer: B 

#P2
##EASY
###BC
8. Which class measures an amount of time using time-based values (seconds, nanoseconds)?

A. Duration
B. Period
C. Seconds
D. Days

Answer: A 

#P1
##EASY
###BC
9. Which is the following syntax helps to get the current time?

A. LocalDate lt = LocalTime.now();  
B. ZonedTime zt = ZonedTime.now();  
C. ZonedDate zd = ZonedDate.now();  
D. LocalDate ld = LocalDate.now();  

Answer: A

#IGNORE
10. ______ is an amount of time that a time-zone varies from Greenwich/UTC. 

A. local-zone offset 
B. datetime-zone offset  
C. time-zone offset 
D. date-zone offset

Answer: C 

#P2
##MEDIUM
###BC
11. Time-zone offset is measured in fixed number of ____ and ______.

A. hours and minutes 
B. minutes and seconds 
C. days and hours
D. None of the above

Answer:A 

#P1
##EASY
###BC
12. Which is the correct syntax to print the current date and time in Java 8?

A. LocalTime currentTime = LocalTime.now(); 
   LocalDate currentDate = LocalDate.now(); 
   LocalDateTime currentDateTime = LocalDateTime.now(); 
B. LocalTime Time = LocalTime.now(); 
   LocalDate currentDate = LocalDate.now(); 
   LocalDateTime currentDateTime = LocalDateTime.now();
C. LocalTime currentTime = LocalTime.now(); 
   LocalDate Date = LocalDate.now(); 
   LocalDateTime currentDateTime = LocalDateTime.now(); 
D. LocalTime currentTime = LocalDateTime.now(); 
   LocalDate currentDate = LocalDateTime.now(); 
   LocalDateTime currentDateTime = LocalDateTime.now(); 

Answer:A 

#P1
##EASY
###BC
13. Choose the output of the following code.
import java.time.*;    
public class LocalDateExample1 {    
  public static void main(String[] args) {    
    LocalDate date = LocalDate.now();   
    System.out.println("Today date: "+date);    
  }    
}

A. It will print the current date
B. It will print the current time
C. It will print the current date and ime
D. None of the above

Answer:A 
