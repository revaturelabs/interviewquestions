HTML
----

1.What is the key difference between HTML Elements and Tags? 
Answer: 
HTML Elements are the sections of the web page, such as a paragraph, an image, or a link is an element whereas HTML Tags are the  elements communicate with the browser how to represent the text and become HTML tags when enclosed within angular brackets <>. 
-------------------------------------------------------------------------------------------------------------------------------

2. What is the use of head tag in HTML?
Answer: 
Which is used to show the title 
-------------------------------------------------------------------------------------------------------------------------------

3. What do you mean by anchor tag
Answer: 
It is used to link two sections, web pages, or website templates in HTML. 
<a href=”#” target=”link”></a> 
-------------------------------------------------------------------------------------------------------------------------------

4. Which image formats can be displayed by all web browsers?
Answer: 
PNG, GIF, JPG
-------------------------------------------------------------------------------------------------------------------------------

5. What is the primary purpose of the <canvas> tag?
Answer: 
     It allows drawing on a bitmap via JavaScript.
-------------------------------------------------------------------------------------------------------------------------------
6. Which attribute is used to get a link to open in a new window or tab
Answer: 
      _blank
-------------------------------------------------------------------------------------------------------------------------------     
7. What is the <hr>tag typically used for?
Answer: 
      It designates a topic shift within a section at the paragraph level.
-------------------------------------------------------------------------------------------------------------------------------     
8. Which tag will you use to convey importance in your text?
Answer: 
      Strong < strong >
-------------------------------------------------------------------------------------------------------------------------------     
9. In the code below, what is the purpose of the id attribute?
<p id="warning">Be careful when installing this product.</p>
Answer: 
It establishes that id is a unique identifier in the document, used for styling CSS, scripting, and linking within a webpage.
-------------------------------------------------------------------------------------------------------------------------------
10. What is the correct markup for alt attribute of an image?
Answer: 
      <img src="cubism.jpg" alt='Version of "Whistler\'s Mother" in cubist style'>
-------------------------------------------------------------------------------------------------------------------------------     
11. What is the difference between the <div> and <span> tags?
Answer: 
      <div> is used where a generic block-level tag is needed, while <span> is used where a generic inline tag is needed.
-------------------------------------------------------------------------------------------------------------------------------     
12. What is the use of <ol> and <ul> elements?
Answer: 
     We can use <ul> when you want a bulleted list and <ol> when you want a numbered list.
-------------------------------------------------------------------------------------------------------------------------------    
13. When is the <link> tag used?
Answer: 
     when linking style sheets, favicons(shortcut icon), and preloading assets
-------------------------------------------------------------------------------------------------------------------------------
14. __________ is a standardized way to indicate the format of the resource content which is sent from the server to the client.
Answer:
    Multipurpose Internet Mail Extension
-------------------------------------------------------------------------------------------------------------------------------
15. The client and server communicate over internet using an application-level protocol known as: ___________
Answer:
      HyperText Transfer Protocol
-------------------------------------------------------------------------------------------------------------------------------
16. Sam wants to create an HTML page. And he wants to display "Bonstay App" to appear on the browser tab. In which section of the HTML code he should include the text "Bonstay App"
Answer: 
     head section
-------------------------------------------------------------------------------------------------------------------------------  
17. While creating HTML page we use various HTML elements. And these HTML elements together form the entire web page. which encloses all other elements in the web page
Answer: 
      <html>
-------------------------------------------------------------------------------------------------------------------------------
18. Joy has created a home page for his application and he has a lot of content in it. Once the user reaches the bottom of the web page, he has to scroll all the way back to the top to see the content in the beginning. 
Is there any provision in HTML to reduce the user's effort in the above scenario?
Answer: 
      Bookmark Hyperlink
-------------------------------------------------------------------------------------------------------------------------------      
19. Riya finds <!DOCTYPE html> at the beginning of all the web page codes.Can you help her to understand the relevance of this?
Answer:
     It declares the version of HTML
-------------------------------------------------------------------------------------------------------------------------------     
20. Which element is used to specify multiple audio/video clips?
Answer:
      <source>
-------------------------------------------------------------------------------------------------------------------------------
21. Which element represents marked or highlighted text for reference purposes?
Answer:
      mark
-------------------------------------------------------------------------------------------------------------------------------
22. Which is the replacement for cookies in HTML5.
Answer:
      Local Storage
-------------------------------------------------------------------------------------------------------------------------------     
23. John has been given an assignment to create a registration form for a Tour and Travel website. He wants to skip the validation for certain input fields. Which attribute of an input element can he use to skip the validation of that field?
Answer:
	formnovalidate
-------------------------------------------------------------------------------------------------------------------------------
24. What is the default method for form submission?
Answer:
       GET
-------------------------------------------------------------------------------------------------------------------------------       
25. In an HTML form, there is an email field for which a validator should be added. Which attribute will help to include regex for email validation?
Answer:
     pattern
-------------------------------------------------------------------------------------------------------------------------------   
26. When using the ______ method, after the submission of the form, the form values will NOT be visible in the address bar of the browser.
Answer:
       POST
-------------------------------------------------------------------------------------------------------------------------------     
27.What are Semantic Elements?
Answer:
Semantic elements are those which has some particular meaning to the browser and the developer. Elements like <form>, <table>, <article>, <figure>, etc., are semantic elements
-------------------------------------------------------------------------------------------------------------------------------
28. What are raster images and vector images?
Answer:
Raster Images - The arrangement of pixels in a grid with exactly what color the pixel should be. Few raster file formats include PNG(.png), JPEG(.jpg), etc.
Vector Images - The algorithms with shape and path definitions that can be used to render the image on-screen written in a similar markup fashion. The file extension is .svg
-------------------------------------------------------------------------------------------------------------------------------
29.What is web storage in html?
Answer:
The web storage is used to store the static data in the local storage of the browser so that we do not need to fetch it from the server every time we need it. There is a size limit based on different browsers. This helps in decreasing the load time and a smooth user experience. There are two types of web storage that are used to store data locally in html:

Local Storage - This helps in storing data that will be retained even though the user reopens the browser. It is stored for each webapp on different browsers.
Session Storage - This is used for one session only. After the user closes the browser this gets deleted.
-------------------------------------------------------------------------------------------------------------------------------
30.What is Image Map?
Answer:
Image Map lets a developer map/link different parts of images with the different web pages. It can be achieved by the <map> tag in html,which we can link images with clickable areas.
-------------------------------------------------------------------------------------------------------------------------------
31.When to use scripts in the head and when to use scripts in the body?

Answer:
If the scripts contain some event-triggered functions then we should use them in the head section. If the script writes the content on the page or is not inside a function then it should be placed inside the body section at the bottom. we have to place library scripts or event scripts in the head section.
and can have normal scripts that do not write anything on the page, in the head section until there is any performance issue.we can place scripts that render something on the web page at the bottom of the body section
-------------------------------------------------------------------------------------------------------------------------------
32.How can you differentiate <strong>, <b> tags and <em>, <i> tags?

Answer:

The effect on a normal webpage of the tags <strong>, <b>  and <em>, <i> is the same. <b> and <i> tags stands for bold and italic. These two tags only apply font styling and bold tag <b>, just adds more ink to the text, these tags don't say anything about the text.Whereas, <strong> and <em> tags represent that the span of text is of more importance  and emphatic stress respectively than the rest of the text.
-------------------------------------------------------------------------------------------------------------------------------
33.How can we club two or more rows or columns into a single row or column in an HTML table?

Answer:
Html provides two table attributes "rowspan" and "colspan" to make a cell span to multiple rows and columns respectively.

How can we display a web page inside a web page or nesting of webpages is possible in html?
Yes, we can display a web page inside another HTML web page. HTML provides a tag <iframe> using which we can achieve this functionality.

<iframe src=" A WebPage inside another webpage "/>
-------------------------------------------------------------------------------------------------------------------------------
34.How can we change an inline element into a block level element?

Answer:
we can change the inline element into a block-level element using the "display" property with its value as "block".
-------------------------------------------------------------------------------------------------------------------------------
35.What is the difference between "display: none" and "visibility: hidden", when used as attributes to the HTML element?
Answer:
When we use the attribute "visibility: hidden" for an HTML element then that element will be hidden from the webpage but still takes up space. Whereas, if we use the "display: none" attribute for an HTML element then the element will be hidden, and also it won’t take up any space on the webpage.


 CSS
 ---
 
35. Which selector is used to specify a rule to bind a particular unique element?
Answer:
	id 
-------------------------------------------------------------------------------------------------------------------------------
36. Using an attribute selector, how would you select an <a> element with a "title" attribute?
Answer:
	a[title]{...}
-------------------------------------------------------------------------------------------------------------------------------	
37. What is the difference between the margin and padding properties?
Answer:
      Margin adds space around an element; padding adds space inside of an element.
-------------------------------------------------------------------------------------------------------------------------------      
38. What is an advantage of using inline CSS?
Answer:
     It is easier to add multiple styles through it.
-------------------------------------------------------------------------------------------------------------------------------     
39. Mike wants to define internal stylesheet. Can you help him to select the appropriate tag?
Answer:
     style
-------------------------------------------------------------------------------------------------------------------------------    
40. What is the correct HTML code to include external stylesheet?
Answer:
    <link rel="stylesheet" type="text/css" href="style.css">
-------------------------------------------------------------------------------------------------------------------------------    
41.What are the different ways which  we can specify the CSS styles for the HTML element?

Answer:
There are three ways in which we can specify the styles for HTML elements:

Inline: we use the <style> attribute inside the HTML element.
Internal: we use the <style> tag inside the <head> tag. To apply the style we bind the elements using 'id' or 'class' attributes.
External:we use the <link> tag inside <head> tag to reference the CSS file into our HTML code. Again the binding between elements and styles is done using 'id' or 'class' attributes.
-------------------------------------------------------------------------------------------------------------------------------
42.Explain the CSS box model

Answer:
The CSS box model contains properties like orders, margin, padding, and the content itself.It is used to create the design and layout of web pages and the web browser supplies each element as a square prism to display the content.
-------------------------------------------------------------------------------------------------------------------------------

43. Which type of CSS holds the highest priority?
Answer:

Inline CSS has the highest priority, then comes Internal/Embedded followed by External CSS which has the least priority. Multiple style sheets can be defined on one page. If for an HTML tag, styles are defined in multiple style sheets then the below order will be followed.

As Inline has the highest priority, any styles that are defined in the internal and external style sheets are overridden by Inline styles.

Internal or Embedded stands second in the priority list and overrides the styles in the external style sheet.

External style sheets have the least priority. If there are no styles defined either in the inline or internal style sheet, then external style sheet rules are applied for the HTML tags.
-------------------------------------------------------------------------------------------------------------------------------

44. List the components of a CSS Style?
Answer: CSS Style is composed of the following elements:

Selector: It is an HTML tag at which a style will be applied.
Property: It is a type of attribute of HTML tag.
Value: These are assigned to properties.
-------------------------------------------------------------------------------------------------------------------------------

45. How to integrate CSS on a web page?
Answer: There are three ways to integrate CSS on a web page:
Inline: To add CSS rules to an HTML element (tag)
Embedded: To add a unique style to a single document.
Linked/Imported/External: To make changes on multiple pages of the website.
-------------------------------------------------------------------------------------------------------------------------------

46. Differentiate between CSS border and outline?
Answer:
CSS border properties allow us to set the style, color, and width of the border. CSS outline property allows us to draw a line around the element, outside the border.
-------------------------------------------------------------------------------------------------------------------------------

47. What are the differences between inline, block and inline block?
Answer:
- inline, elements do not break the flow. think of span it fits in the line. Important points about inline elements, margin/ padding will push other elements horizontally not vertically. Moreover, inline elements ignore height and width.

- block, breaks the flow and don’t sit inline. they are usually container like div, section, ul and also text p, h1, etc.

- inline block, will be similar to inline and will go with the flow of the page. Only differences are, this will take height and width.
-------------------------------------------------------------------------------------------------------------------------------

48. What is the difference between padding and margin?
Answer:
1) Margin is applied to the outside of your element hence effecting how far your element is away from other elements.
2) Padding is applied to the inside of your element hence effecting how far your element's content is away from the border.
-------------------------------------------------------------------------------------------------------------------------------

49. Does margin-top or margin-bottom have an effect on inline elements?
Answer:
No, it doesn’t affect the inline elements. Inline elements flow with the contents of the page.
-------------------------------------------------------------------------------------------------------------------------------

50. How do I restore the default value of a property?
Answer:
The keyword initial can be used to reset it to its default value.

JavaScript
----------

51. Mention the different data types present in JavaScript.
Answer:

There are two types of data types to hold different types of values in JavaScript.
Primitive types- String, Number, Boolean, Undefined, Null
Non-primitive (reference) data type - Object, Array, RegExp
-------------------------------------------------------------------------------------------------------------------------------
52. Do we need to specify type of the variable in JavaScript?
Answer
No, JavaScript is a dynamic type language , so no need to specify type of the variable because it is dynamically used by JavaScript engine

-------------------------------------------------------------------------------------------------------------------------------
 
 53. Explain about the scopes of a variable in JavaScript?
Answer:
The scope of a variable represents where the variable has been declared or defined in a JavaScript program. There are two scopes of a variable:
Global Scope -Global variables, having global scope are available everywhere in a JavaScript code.
Local Scope -The Local Scope has divided into the Function scope, the Block scope, and the Lexical scope.
  Function Scope -The variable declared in a function is only visible inside that function.
  Block Scope -Block scope is the scope of the variables declared inside the {} (curly brackets).
  Lexical Scope -Lexical scope is that a variable defined outside a function can access the inside another 		   
  function defined after the variable declaration. The inner functions are lexically bound to the execution context of their  
  outer functions.
  
54. Explain Hoisting in JavaScript.
Answer:
Hoisting is the default behavior of JavaScript where all the variable and function declarations are moved on top, irrespective of where the variables and functions are declared, they are moved on top of the scope. The scope can be both local and global.

55. When should we use ‘for loop’  in JavaScript?
Answer:
It iterates the elements for the fixed number of times. It should be used if the number of iterations is known.

56. 

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 Coding
 ------
 
 1. What will be the output of the following HTML code when it is rendered in the browser?
<!DOCTYPE html>
<html>
     <body>
	 
          Hello<World!
     </body>
</html>
Answer: 
       Hello is displayed in browser
-------------------------------------------------------------------------------------------------------------------------------       
2. John has created his first HTML page, when he runs it, the web page is displaying an incorrect title. Can you help him out by suggesting an appropriate solution by selecting an option.
<!DOCTYPE html>
<html>
    <title>
        <head>Testing</head>
    </title>
    <body>
        Welcome to Infosys!!<br> All the best!
    </body>
</html>
Answer: 
   <title> tag must be placed inside <head> element
-------------------------------------------------------------------------------------------------------------------------------   
3. Sam is developing a web application to display cricket scores of live matches. He wants the web page to get auto-refreshed after every 30 seconds. Which of the following attributes of meta tag will help him to achieve this requirement?
<html>
	<head>
		<meta _________________ />
	</head>
	<body>
		…
	</body>
</html>
Answer: 
    http-equiv = "refresh" content = "30"
-------------------------------------------------------------------------------------------------------------------------------   
4. What happens when a user submits the form with an invalid email format?
<form name="register" action="success.html" novalidate="true">
    <input type="email" name="emailid">
    <input type="submit">
</form>
Answer: 
      Form will be submitted successfully and will redirect to success.html
-------------------------------------------------------------------------------------------------------------------------------     
5. Alex want to make a webpage which will contain a form with some fields like Name, password, confirm password, date of birth and email id.What he has to code in line 1 for succesfull compilation?
//Line 1
Name<input type="text" id="name" > 
Password<input type="password" id="password"> 
Email ID<input type="email" id="email"> 
<button onclick="submit" >Submit</button> 
</form>
Answer: 
       <form name="form" action="success.html" method="post"> 
-------------------------------------------------------------------------------------------------------------------------------
6. In the example below, when will the color pink be applied to the anchor element?
a:active {
  color: pink;
}
Answer: 
The color of the link will display as pink while the link is being clicked but before the mouse click is released.
-------------------------------------------------------------------------------------------------------------------------------
7. How would you make the first letter of every paragraph on the page red?
Answer: 
     =p::first-letter { color: red; }
-------------------------------------------------------------------------------------------------------------------------------     
8. Thomas has developed a website, where he has already added the box-shadow property. Now, if he wants to blur the shadow applied, which value among the below should be changed?
box-shadow: 10px 5px 5px 10px rgba(30,40,50,0.4)
Answer:
     Lower the third value => 5px
-------------------------------------------------------------------------------------------------------------------------------    
 9. Alex wanted to apply the border-radius property to an element such that only the top-right and bottom-left corners will be rounded. Which of the following option can be used by Alex?
Answer:
      border-radius: 0 12px 0 12px;

-------------------------------------------------------------------------------------------------------------------------------
10.How the images can be responsive in html?
Answer:
we can use <picture> element to make image responsive.

<picture>
 <source media="(min-width: 500px)" srcset="india.jpg">
 <img src="india.jpg" style="width:auto;">
</picture>
-------------------------------------------------------------------------------------------------------------------------------
11.How the novalidate attribute can be used in the form tag in html code
Answer:
The attibute value is a boolean type that indicates whether or not the data being submitted by the form will be validated beforehand. By making this false, forms can be submitted without validation which helps users to resume later also.

<form action = "" method = "get" novalidate>
       Name:<br><input type="name" name="firstname"><br>
       Number:<br><input type="number" name="dnumber"><br>
        <input type="submit" value="Submit">
</form>

-------------------------------------------------------------------------------------------------------------------------------

12.How the media elements tags can be used in html?
Answer:
<audio> - It is used for sounds, audio streams, or music, and embed audio content.
<video> - It is used for video streams, and  embed video content etc.
<source> - It is used for multiple media resources in media elements, such as audio, video, etc.
<embed> - It is used for an external application or embedded content.
<track> - It is used for subtitles in the media elements such as video or audio.
<label>
       Video:
   </label>
    <video width="320" height="240" controls>
        <source src="video1.mp4" type="video/mp4">
        <track src="sub.vtt" kind="subtitles" srclang="en" label="English">
    </video>
    <br>
    <label>
        Embed:
    </label>
    <embed type="video/webm" src="https://www.youtube.com/embed/KPpKOIYU1ew" width="400" height="300">
    <br>
    <label>
        Audio:
    </label>
    <audio controls>
        <source src="audio1.mp3" type="audio/mpeg">
    </audio>
-------------------------------------------------------------------------------------------------------------------------------
13.How to insert a picture of in a web page as a background image?

Answer:
To make a picture a background image on a web page, you should put the following tag code after the </head> tag.

<body background = "google.gif">
Here, replace the "google.gif" with the name of your image file which you want to display on your web page
-------------------------------------------------------------------------------------------------------------------------------
14. How can the background color of an element be changed?
Answer:
The background color of an image can be changed using the background-color property.

body
{
background-color: coral;
}
Javascript
-----------

14.How can you make an image draggable in html?
Answer:
The native drag and drop API, this was achievable by writing Javascript programming or external frameworks like jQuery.To enable this functionality there is a draggable attribute in the <img> tag and need to set ondrop and ondragover attribute to an eventhandler available in scripts.

<!DOCTYPE HTML>
<html>
 <head>
   <script>
     function allowDrop(dd) {
       dd.preventDefault();
     }
     function drop(dd) {
       ...
     }
   </script>
 </head>
 <body>
   ...
   <div id="div1" ondrop="drop(event)" ondragover="allowDrop(event)" style="border: 3px solid #aaaaaa; width:400px; height: 100px;"></div>
   <br>
   <img id="drag1" src="img1.gif" draggable="true" width="450" height="75">
    ...
 </body>
</html>
-------------------------------------------------------------------------------------------------------------------------------
15.How to handle events in HTML using javascript?

Answer:
Html allows event trigger actions in browsers using javascript. There are a lot of events like 'onclick', 'ondrag', 'onchange', etc.

<!DOCTYPE html>
<html>
   <body style="padding-top:30px">
       <h3 id="demo_event">1</h3>
       <input type="button" onclick="Function1()" value="Click on Events" />
       <input type="reset" onclick="reset()" value="Reset" />
   </body>
   
   <script>
       function myFunction() {
           var val = document.getElementById("demo_event").innerHTML
           val = parseInt(val) + 1;
           document.getElementById("demo_event").innerHTML = val;
       }
       function reset() {
           document.getElementById("demo_event").innerHTML = 0;
       }
   </script>
</html>
-------------------------------------------------------------------------------------------------------------------------------
16.how to get the location - latitude and logitude position using html and javascript code

Answer:
<!DOCTYPE html>
<html>
  <body>
     <p>Click this button to get your coordinates.</p>
     <button onclick="getLoc()">Try It</button>
     <p id="getlocation"></p>
     <script>
        var a = document.getElementById("locationdemo");
        
        function getLoc() {
          if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition(showPosition);
          } else { 
            a.innerHTML = "The location can be identified by longitude and latitude";
          }
        }
        
        function showPosition(position) {
          a.innerHTML = "Latitude: " + position.coords.latitude + 
         "<br>Longitude: " + position.coords.longitude;
        }
     </script>
  </body>
</html>

17.How the Web components can be used in Html using javascript?
Answer:
	The Web components are composed of javascript API's to define custom elements,shadowing DOM tree and html templates.
	<!DOCTYPE html>
<html>
 <head>

<title>Web components</title>
<script src="web.js" defer></script>
 </head>
 <body>
<h1><code>html web components</code></h1>
<open-shadow text="Shadowing - open "></open-shadow>
<closed-shadow text="Shadowing - closed "></closed-shadow>
 </body>
</html>

customElements.define('open-shadow',class extends HTMLElement {
constructor() {
  super();
  const elem= document.createElement('p');
  elem.textContent = this.getAttribute('text');
  const shadroot = this.attachShadow({mode: 'open'});
  shadroot.appendChild(elem);
}
 }
);
customElements.define('closed-shadow',class extends HTMLElement {
constructor() {
  super();
  const elem = document.createElement('p');
  elem.textContent = this.getAttribute('text');
  const shadroot = this.attachShadow({mode: 'closed'});
  shadroot.appendChild(elem);
}
 }
);
document.querySelector('html').addEventListener('click', e => {
 console.log(e.composed);
 console.log(e.composedPath());
});


<!DOCTYPE html>
<html>
<head>
 <title>html template</title>
 <script src="temp.js"></script>
</head>
<body>
 <h1>web template</h1>
 <template id="html-para">
<style>
  p {
    color: white;
    background-color: #666;
    padding: 5px;
  }
</style>
<p><slot name="html-text">Html text</slot></p>
 </template>
 <html-para>
<span slot="html-text">Html template text</span>
 </html-para>
 <html-para>
<ul slot="html-text">
  <li>html web text!</li>
  <li>html list!</li>
</ul>
 </html-para>
</body>
</html>
customElements.define('html-para',
 class extends HTMLElement {
constructor() {
  super();
  const temp = document.getElementById('my-paragraph');
  const tempContent = template.content;
  this.attachShadow({mode: 'open'}).appendChild(
    tempContent.cloneNode(true)
  );
}
 }
);
const slottedSpan = document.querySelector('my-para span');
console.log(slottedSpan.assignedSlot);
console.log(slottedSpan.slot);
-------------------------------------------------------------------------------------------------------------------------------
18.How to include javascript code in HTML?

Answer:
Html provides a <script> tag using which we can run the javascript code and make our Html page more dynamic.

<!DOCTYPE html>
<html>
   <body>
    <h1>
          <span>Javascript page </span>
          <u><span id="javascript demo"></span></u>
   </h1>
   <script>
       document.getElementById("javascript demo").innerHTML = "Javascript tag"
   </script>
   </body>
</html>
-------------------------------------------------------------------------------------------------------------------------------
What are the three main ways to apply CSS styles to a web page?
Answer
Using the inline style attribute on an element
<div>
    <p style="color: maroon;"></p>
</div>
Using a <style> block in the <head> section of your HTML
<head>
    <title>CSS Refresher</title>
    <style>
        body {
            font-family: sans-serif;
            font-size: 1.2em;
        }
    </style>
</head>
Loading an external CSS file using the <link> tag
<head>
    <title>CSS Refresher</title>
    <link rel="stylesheet" href="/css/styles.css" />
</head>



