1. What is a container or object that combines multiple elements into a single unit and is used to store, retrieve and manipulate data.
    A. Collection
    B. Abstract
    C. Container framework
    D. Singleton

Answer: A

2. Collection framework contains _______ for all the data structures?
    A. Irohibition
    B. Inhibition
    C. Implementations
    D. Interruption

Answer: C
 
3. All the classes under the collection framework implement an _______ interface.
    A. Comparator
    B. Iterable
    C. Considerable
    D. Deliverable

Answer: B

4. Fill in the code from given choices. 

import java.util.*;
public class Main {
    public static void main(String[] args) {
        ArrayList<Integer> al = new ArrayList<>();
        al.add(1);
        al.add(13);
        al.add(2);
        al.add(0);
        //fill here
        {
            System.out.println(i);
        }
    }
}


    A. for(Integer i:al)
    B. for(String i: al)
    C. foreach(int i:al)
    D. foreach(Integer i:al)

Answer: A
 
5. Fill in the code from given choices. 

import java.util.*;
public class Main {
    public static void main(String[] args) {
        ArrayList<Integer> al = new ArrayList<>();
        al.add(1);
        al.add(13);
        al.add(2);
        al.add(0);
        //Fill here
        while(i.hasNext())
        {
            System.out.println(i.next());
        }
    }
}

    A. Iterator al = new iterator();
    B. Iterator i = al.iterator(new);
    C. Iterator i = new iterator();
    D. Iterator i = al.iterator();

Answer: D
 
6. Fill in the code from given choices. 

import java.util.*;
public class Main {
    public static void main(String[] args) {
        ArrayList<Integer> al = new ArrayList<>();
        al.add(1);
        al.add(13);
        al.add(2);
        al.add(0);
        al.stream()._______________________);
    }
}


    A. forEach(e--> System.out.println(e)
    B. forEach(e-> System.out.println(e)
    C. forEach(e.System.out.println(e)
    D. forEach(e(System.out.println(e))

Answer: B

7. A comparable object is capable of comparing itself with another object and the comparator class must implement the java.util.Comparator interface. 
    A. True
    B. False

Answer: A

8. The concurrent collection APIs of Java provides a range of classes that are specifically designed to deal with concurrent operations. 
    A. True
    B. False

Answer: A

9 Using Enumeration, you can traverse the collection and also do any modifications to the collection while traversing it. 
    A. True
    B. False

Answer: B

10. Which command is used to arrange elements in random order.

    A. sort(Collection)
    B. random(Collection)
    C. shuffle(Collection)
    D. reverse(Collection)

Answer: C
